apiVersion: apps/v1
kind: Deployment
metadata:
  name: ocm-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: ocm-operator
  template:
    metadata:
      labels:
        name: ocm-operator
    spec:
      serviceAccountName: ocm-operator
      containers:
        - name: ocm-operator
          # Replace this with the built image name
          image: REPLACE_IMAGE
          command:
          - ocm-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "ocm-operator"
